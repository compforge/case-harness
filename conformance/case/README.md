# Canonical CaseSet conformance

`fixtures/shared-chat.yaml` is one spec-case CaseSet consumed unchanged by Python Eval and
TypeScript Perf tests. Harness experiments may select cases and add run parameters, but may not
redefine Case input, facets, sources, or per-face judgment.
